import 'package:flutter/material.dart';

class kConstants {
  final TextStyle dtextDrawer = TextStyle(fontSize: 25, color: Colors.red);
  // TextStyle buildTextStyleDrawer() => TextStyle(fontSize: 25);
}
